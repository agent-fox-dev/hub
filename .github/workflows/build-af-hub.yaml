name: build af-hub container
on:
  workflow_dispatch:

  pull_request:
    branches:
      - 'main'
  push:
    branches:
      - 'main'
jobs:
  build-runtime-container:
    uses: ./.github/workflows/build.yaml
    with:
      container_name: hub
    secrets: inherit
    permissions:
      packages: write
      contents: read
      id-token: write
      attestations: write
